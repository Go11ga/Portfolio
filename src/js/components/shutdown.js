/* Shutdown links */
$(function(){
    $('#loadMore').on('click', function(e){
        e.preventDefault();
    })

    $('.articles__header a').on('click', function(e){
        e.preventDefault();
    })

    $('.articles__title a').on('click', function(e){
        e.preventDefault();
    })

    $('.read-more').on('click', function(e){
        e.preventDefault();
    })

    $('#sendBtn').on('click', function(e){
        e.preventDefault();
    })

}());

